# Plan

1. Replace the long marketing homepage with a concise product shell.
2. Extract homepage CSS/JS into modular assets and apply accessibility hardening.
3. Replace the static demo bridge with a fail-closed configurable API bridge.
4. Make the widget honest about connectivity and corpus evidence.
5. Update Pages build/verification and CI-facing tests.
6. Record the public-query/BFF boundary and GCP target architecture.
7. Add a named hard eval, run full regression, detached verification, publish,
   observe CI, and reconcile program state.
